import React, {Fragment} from 'react';


function LoginPage () {

    return (
        <Fragment>
            <h1>LoginPage</h1>



               {/* <div className="row mt-5">
                    <div className="mt-4 col-12">

                        {error !== "" ?
                            <TextField id="outlined username" htmlFor="username" name="username"
                                       label="Email" style={{margin: 8}} placeholder="Votre adresse email"
                                       className="loginInputs is-invalid"
                                       helperText={error}
                                       margin="normal"
                                       InputLabelProps={{
                                           shrink: true,
                                       }}
                                       variant="outlined"
                                       error
                            />
                            :
                        <TextField id="outlined username" htmlFor="username" name="username"
                            label="Email" style={{ margin: 8 }} placeholder="Votre adresse email"
                            className="loginInputs is-invalid" helperText="Utilisez votre adresse email @savpro.com"
                            margin="normal"
                            InputLabelProps={{
                                shrink: true,
                            }}
                            variant="outlined"
                            onChange={handleChange}

                        />}
                    </div>
                </div>
                <div className="row">
                    <div className="mt-5 col-12">
                        <TextField
                            id="outlined-password-input"
                            htmlFor="password"
                            name="password"
                            className="loginInputs"
                            label="Password"
                            type="password"
                            autoComplete="current-password"
                            placeholder="Votre mot de passe"
                            variant="outlined"
                            onChange={handleChange}
                        />
                    </div>
                </div>
                <div className="row mt-4">
                    <div className="mt-4 col-12">
                        <ButtonGroup size="large" aria-label="large outlined primary button group"
                                     className="loginButton">
                            <Button type="submit">Connexion</Button>
                        </ButtonGroup>
                    </div>
                </div>*/}



</Fragment>
    );
}

export default LoginPage;